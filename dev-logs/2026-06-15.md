# 开发日志

## 日期：2026-06-15

### 今日完成
- [x] **账户安全功能** — 找回密码 / 修改密码 / 修改邮箱（邮箱验证）

### 服务端（Phase 1-3）

**Phase 1 — 数据库**
- User 模型新增 5 个可选字段：`resetToken`、`resetTokenExpiresAt`、`emailChangeToken`、`emailChangeTokenExpiresAt`、`pendingEmail`
- 执行 Prisma 迁移：`add_auth_token_fields`

**Phase 2 — 工具层**
- 新建 `server/src/lib/tokens.ts` — `generateToken()` 使用 `crypto.randomBytes(32)` 生成安全随机令牌，`TOKEN_EXPIRY_MS = 3600000`（1 小时）
- 新建 `server/src/lib/email.ts` — nodemailer 封装，SMTP 未配置时仅 console.log 打印验证链接（开发环境可用）

**Phase 3 — Auth 模块**
- `auth.schema.ts`：新增 5 个 Zod schema
- `auth.service.ts`：新增 5 个函数 — `forgotPassword`（防邮箱枚举）、`resetPassword`（token 验证+过期检查）、`changePassword`（需旧密码）、`requestEmailChange`（需密码+邮箱唯一检查）、`confirmEmailChange`（token 验证+邮箱切换）
- `auth.controller.ts`：新增 5 个 handler
- `auth.routes.ts`：新增 5 条路由（3 公开 + 2 需认证）

### 前端（Phase 4-5）

**Phase 4 — 新页面**
- 新建 `client/src/pages/ForgotPassword.tsx` — 输入邮箱 → 发送重置邮件
- 新建 `client/src/pages/ResetPassword.tsx` — 从 URL 读取 token，设置新密码
- 新建 `client/src/pages/AccountSettings.tsx` — 账户信息 + 修改密码 + 修改邮箱表单，退出登录按钮
- 新建 `client/src/pages/ConfirmEmailChange.tsx` — 验证邮箱变更 token
- `client/src/api/auth.ts`：新增 5 个 API 函数

**Phase 5 — 路由集成**
- `App.tsx`：新增公开路由 `/forgot-password`、`/reset-password`、`/confirm-email`；受保护路由 `/account`
- `Sidebar.tsx`：用户菜单「写作设置」→「账户设置」，导航至 `/account`
- `Login.tsx`：密码框下方增加「忘记密码？」链接

### SMTP 配置与邮件优化
- `server/.env`：配置 QQ 邮箱 SMTP（3407835646@qq.com），邮件真实发送
- `server/src/lib/email.ts` 重构：
  - 支持多 SMTP 提供者自动选择（根据发件地址域名匹配 QQ/163）
  - 新增 `getSmtpConfigs()` 导出，服务启动时打印 SMTP 状态
  - 新增 `emailTemplate()` — 规范化 HTML 邮件模板（居中卡片、紫色按钮、纯文本链接兜底）
  - 新增 `sendResetEmail()` 和 `sendEmailChangeEmail()` 快捷发送函数
  - SMTP 发送增加 try/catch，错误打印到控制台便于调试
- `server/src/index.ts`：启动时打印 SMTP 配置状态
- `auth.service.ts`：改用新的 `sendResetEmail()` / `sendEmailChangeEmail()` 替换裸 `sendEmail()` 调用
- `FRONTEND_URL` 从 localhost 改为局域网 IP `10.67.139.169:5173`，同 WiFi 内多设备可点击邮件链接
- 探索了 localtunnel、serveo 等内网穿透方案，免费版均有验证页/限制，决定开发期使用局域网 IP，正式上线后使用真实域名

### 修复
- `auth.service.ts` 中导入的 `generateToken` 与本地 JWT `generateToken` 命名冲突 → 重命名导入为 `generateRandomToken`
- TypeScript 检查：client 零错误，server 零错误
